# OpenCores Ethernet MAC driver (QEMU only)
CONFIG_ETH_USE_OPENETH=y
# 让 outbox 过期删除可见：每条被丢弃的消息发一条 MQTT_EVENT_DELETED
CONFIG_MQTT_USE_CUSTOM_CONFIG=y
CONFIG_MQTT_REPORT_DELETED_MESSAGES=y
CONFIG_MQTT_OUTBOX_EXPIRED_TIMEOUT_MS=90000
