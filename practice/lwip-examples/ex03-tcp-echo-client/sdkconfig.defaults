# 必填：OpenCores Ethernet MAC 驱动（仅 QEMU 可用，真机不可用）
CONFIG_ETH_USE_OPENETH=y

# 说明：本示例不再叠加其他开关。
# - SO_RCVTIMEO 能力由 IDF port 硬编码提供（LWIP_SO_RCVTIMEO=1，
#   components/lwip/port/include/lwipopts.h），无需 Kconfig。
