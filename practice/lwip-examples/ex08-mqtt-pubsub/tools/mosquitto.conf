# ex08 demo broker 配置：允许匿名、监听 1883、不落盘
# SLIRP 下 guest 访问 10.0.2.2:1883 会落到宿主机 loopback 同端口，
# 因此监听全部接口即可（同时方便 mosquitto_sub/pub 从 127.0.0.1 观察）。
listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_dest stdout
